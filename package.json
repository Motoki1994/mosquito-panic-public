{
  "name": "mosquito-panic",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "release:check": "node scripts/check-public-dist.cjs",
    "release:prepare": "npm run build && node scripts/prepare-public-release.cjs"
  },
  "dependencies": {
    "phaser": "^3.60.0"
  },
  "devDependencies": {
    "typescript": "^5.3.0",
    "vite": "^5.0.0"
  }
}
